name: RedCameraTest
options:
  bundleIdPrefix: vn.rockettracker
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "9.30"
    CURRENT_PROJECT_VERSION: "109"
targets:
  RedCameraTest:
    type: application
    platform: iOS
    sources:
      - path: RedCameraTest/RedCameraTestApp.swift
      - path: RedCameraTest/ContentView.swift
      - path: RedCameraTest/H2DBLEManager.swift
      - path: RedCameraTest/H2DTimelapseManager.swift
      - path: RedCameraTest/H2DTimelapseView.swift
      - path: RedCameraTest/PrinterAlarmPlayer.swift
      - path: RedCameraTest/BambuPrinterProfile.swift
      - path: RedCameraTest/BambuCredentialStore.swift
      - path: RedCameraTest/Assets.xcassets
      - path: RedCameraTest/fire-alarm-sound.mp3
    info:
      path: RedCameraTest/Info.plist
      properties:
        CFBundleDisplayName: SE
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        LSRequiresIPhoneOS: true
        NSBluetoothAlwaysUsageDescription: Kết nối ESP32 để nhận tín hiệu từng lớp từ máy in Bambu.
        NSCameraUsageDescription: Chụp từng lớp để tạo timelapse máy in Bambu.
        NSPhotoLibraryAddUsageDescription: Lưu video timelapse máy in Bambu vào ứng dụng Ảnh.
        UILaunchScreen: {}
        UIRequiredDeviceCapabilities:
          - arm64
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: vn.rockettracker.RedCameraTest
        MARKETING_VERSION: "9.30"
        CURRENT_PROJECT_VERSION: "109"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_STYLE: Automatic
        TARGETED_DEVICE_FAMILY: "1"
